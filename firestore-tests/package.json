{
  "name": "capacify-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Emulator tests for firestore.rules. Run with: npm run test:emulator (needs a JDK on PATH for the Firestore emulator).",
  "scripts": {
    "test": "node --test --test-concurrency=1",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-capacify --config ../firebase.json \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0"
  }
}
